{
  "name": "nuevo_front",
  "version": "0.1.0",
  "private": true,
  "description": "Prototipo visual del index (login) de PLATAFORMA-OMAS. Dark Futurist + Motion + TypeScript.",
  "scripts": {
    "build": "esbuild src/app.ts --bundle --minify --outfile=dist/app.js --format=iife --target=es2019",
    "watch": "esbuild src/app.ts --bundle --outfile=dist/app.js --format=iife --target=es2019 --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0",
    "@types/three": "^0.169.0"
  },
  "dependencies": {
    "motion": "^11.11.0",
    "three": "^0.169.0"
  }
}
